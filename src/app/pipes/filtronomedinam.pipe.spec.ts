import { FiltronomedinamPipe } from './filtronomedinam.pipe';

describe('FiltronomedinamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltronomedinamPipe();
    expect(pipe).toBeTruthy();
  });
});
